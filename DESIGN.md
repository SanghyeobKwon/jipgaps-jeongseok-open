# 집값의 정석 — Metropolitan Atlas & Decision Dossier

## Mode

Operate. 사용자가 대한민국 전체에서 한 동·한 단지까지 좁혀 가격과 생활 근거를 검토하는 작업형 제품 UI다.

## Thesis

지역 탐색을 지도 위 선택과 우측 의사결정 기록이 맞물리는 하나의 작업대로 만든다. 부동산 포털의 카드 모음이나 광고성 매물 피드처럼 보이지 않아야 한다.

## Visual World

수도권 환승 지도와 정돈된 주택 조사 기록지가 만나는 세계다. 따뜻한 회백색 캔버스 위에 종이처럼 선명한 흰 작업면을 놓고, 현재 선택 대상은 잉크 남청색 인스펙터로 고정한다. 행동은 선명한 블루, 확인된 생활 근거는 절제된 민트로 구분한다.

## First Viewport

얇은 상단 내비게이션 아래에 시·도, 시·군·구, 읍·면·동, 단지를 한 흐름으로 고르는 명령 바가 있다. 그 아래에는 넓은 지도와 선택 지역 인스펙터가 나란히 배치된다. 사용자는 첫 화면에서 현재 위치, 주변 맥락, 다음 행동을 즉시 이해한다.

## Palette

- Canvas: `#f3f6fa`
- Primary surface: `#ffffff`
- Primary ink: `#0c1c32`
- Secondary ink: `#617086`
- Hairline: `#d9e1eb`
- Action blue: `#155bd7`
- Action tint: `#eaf2ff`
- Evidence mint: `#0d9f8a`
- Selected dossier: `#0d2038`
- Negative: `#c24132`

다크 모드는 캔버스 `#08111e`, 작업면 `#0d1a2a`, 경계 `#25354a`, 밝은 행동 블루 `#67a6ff`를 사용한다.

## Typography

Apple SD Gothic Neo, Pretendard, Noto Sans KR 계열을 우선한다. 제목은 짧고 조밀하게, 본문은 13px 이상으로 유지한다. 가격과 거래량은 tabular numerals로 정렬한다. 영문 대문자 라벨과 모노스페이스는 실제 데이터 표기 외에는 사용하지 않는다.

## Composition

콘텐츠 최대 폭은 1344px이다. 검색기는 하나의 연속된 명령 바로, 지도와 선택 정보는 2단 작업면으로 구성한다. 요약·차트·거래·임장은 같은 크기의 카드 반복 대신 표, 리스트, 넓은 단일 표면과 접이식 행을 우선한다. 테두리와 그림자를 동시에 남용하지 않는다.

## Interaction

활성 탭은 블루 글자와 이동하는 3px 표시선으로 구분한다. 지도 단계는 남청색 채움, 주요 행동은 블루 채움으로 표시한다. 키보드 포커스는 반투명 3px 블루 링을 사용한다. 지도 선택은 줌을 불필요하게 초기화하지 않고 우측 인스펙터와 관련 분석만 갱신한다.

## Responsive

모바일에서는 검색기를 2열 뒤 단지 검색과 실행 버튼을 전체 폭으로 배치한다. 지도 다음에 선택 인스펙터가 이어지고, 현재 위치와 다음 행동은 첫 화면에 남는다. 주요 터치 영역은 최소 44px이며, 핵심 기능을 수평 스크롤에만 숨기지 않는다.

## State Language

로딩, 오류, 데이터 없음, 표본 부족을 각각 다른 상태로 표현한다. 실제 연결 데이터가 아닌 값은 `예시`, `추정`, `베타` 중 정확한 상태를 붙인다. 가격 평가는 투자 권유가 아니라 탐색 참고임을 유지한다.

사용자 화면의 기능명과 상태는 자연스러운 한국어 문장으로 쓴다. 내부 구현 용어, 영문 대문자 장식, 원본 오류 메시지는 노출하지 않으며 오류 상태에는 문제 요약과 다시 시도할 행동을 함께 둔다.

## Evidence Policy

출처가 확인되지 않은 점수와 추천 수치는 만들지 않는다. 데이터 연결 전에는 평가 항목과 계산 기준만 보여주고, 실제 값이 있는 항목만 결과로 표시한다. 정책 영향은 `호재·악재`처럼 단정하지 않고 `선택지 확대`, `부담 가능`, `영향 혼재`처럼 범위를 드러낸다.

## External Design References

- Figma: `집값의 정석 — Product Redesign 2026`
- Lovable: `Home Compass Pro` 실험 프로젝트

두 외부 시안은 구조와 밀도 검토용이며, 제품의 실제 데이터 로직과 문구 정책은 이 저장소를 기준으로 한다.
