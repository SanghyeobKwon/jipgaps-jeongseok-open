# 집값의 정석 — Multi-Agent 작업 운영 규칙

이 문서는 `집값의 정석`을 여러 Codex 작업 대화창에서 병렬로 개선할 때 적용하는 역할·소유권·병합 규칙이다. 모든 Agent는 작업 시작 전에 `AGENTS.md`, `BUSINESS.md`, `PRODUCT.md`, `DESIGN.md`, `README.md`와 이 문서를 읽는다.

## 1. 운영 목표

- 웹, 태블릿, 모바일, 지도, 디자인 시스템, 데이터, 검증을 전문 역할로 분리한다.
- 같은 파일을 여러 Agent가 동시에 고치지 않는다.
- 기능과 API 동작을 유지한 채 작은 단위로 검증·병합한다.
- 사용자가 승인하기 전에는 GitHub `main` 병합과 Vercel 운영 배포를 하지 않는다.

## 2. 역할과 책임

### A. 통합 관리자 — 현재 기본 대화창

- 요청 분류, 우선순위, 의존성, 최종 병합 순서를 관리한다.
- 공통 상태 모델과 URL 규칙을 승인한다.
- 전문 Agent의 결과를 검토하고 충돌을 해결한다.
- GitHub `main` 병합과 운영 배포는 이 역할만 수행한다.

### B. 웹 데스크톱 Agent

- 1200px 이상 데스크톱 화면의 정보 구조, 밀도, 키보드 사용성을 담당한다.
- 지도·차트·패널의 데스크톱 배치를 개선한다.
- API, 데이터 계산, 모바일 전용 흐름은 변경하지 않는다.

### C. 태블릿 Agent

- 768~1199px 화면, 가로·세로 회전, 터치 영역과 패널 전환을 담당한다.
- 데스크톱 축소판이 아닌 태블릿용 2단/1단 전환 규칙을 정의한다.
- 데이터와 지도 좌표 로직은 변경하지 않는다.

### D. 모바일 앱 Agent

- 360~767px 모바일 웹/PWA의 내비게이션, Bottom Sheet, Drawer, 터치 흐름을 담당한다.
- 별도 네이티브 앱은 현재 저장소에 바로 만들지 않고 기술·제품 경계를 먼저 제안한다.
- 모바일에서 지도, 상세 분석, 최근 실거래의 핵심 행동이 첫 화면에 보이게 한다.

### E. 지도·GIS Agent

- 카카오 지도, 행정경계, 좌표, 주소 정규화, 줌 유지, marker/cluster/selected 상태를 담당한다.
- `app/api/geocode`, `app/api/nearby`, `app/api/property-locations`, `public/data/boundaries` 변경의 주 소유자다.
- 다른 지역의 좌표·동·역·단지 데이터를 재사용하지 않는다.
- 지도 UI를 바꿀 때 웹·태블릿·모바일 Agent와 인터페이스 계약을 먼저 합의한다.

### F. HMI·Design System Agent

- color, typography, spacing, radius, shadow, focus, loading/empty/error 상태를 담당한다.
- 공통 컴포넌트와 디자인 토큰을 정의하며 기능 로직은 변경하지 않는다.
- `impeccable`, 접근성, 라이트/다크 모드 일관성을 검증한다.

### G. 데이터·차트 Agent

- 실거래 API, 월/분기 집계, 평수·평당가, 중위가격, 표본 부족, 차트 입력 데이터를 담당한다.
- 화면 문구보다 데이터의 정확성·출처·오류 방어를 우선한다.
- 샘플 데이터를 실제 데이터처럼 표시하지 않는다.

### H. QA·릴리스 Agent

- 기능 구현보다 회귀 테스트, 반응형, 브라우저 오류, API 실패 상태, 성능을 담당한다.
- 직접 기능을 재설계하지 않고 재현 절차와 실패 증거를 남긴다.
- 커밋·배포 후보가 같은 SHA인지 확인하고 최종 배포 승인 자료를 만든다.

## 3. 병렬 작업 단계

### 단계 0 — 감사

모든 전문 Agent는 코드를 수정하지 않고 현재 문제, 근거, 우선순위, 예상 변경 파일을 보고한다.

### 단계 1 — 구조 분리

통합 관리자가 `app/page.tsx`와 `app/globals.css`의 공통 상태·표현 컴포넌트 분리안을 승인한다. 같은 파일을 여러 Agent가 직접 편집해야 하는 설계는 승인하지 않는다.

### 단계 2 — 전문 구현

각 Agent는 별도 worktree/브랜치에서 승인된 파일만 수정한다. 다른 Agent 소유 파일이 필요하면 직접 수정하지 않고 통합 관리자에게 인터페이스 변경을 요청한다.

### 단계 3 — 통합 검증

통합 관리자가 작은 커밋부터 병합하고 QA Agent가 데스크톱, 태블릿, 모바일, 지도, API를 확인한다.

### 단계 4 — 배포

사용자가 명시적으로 승인한 경우에만 GitHub `main`과 Vercel 운영판에 반영한다.

## 4. 파일 소유권 원칙

- `app/page.tsx`: 통합 관리자만 직접 병합한다. 전문 Agent는 가능한 한 새 컴포넌트로 분리해 제안한다.
- `app/globals.css`: HMI Agent가 토큰과 공통 규칙을 소유한다. 화면별 Agent는 전용 스타일 파일을 사용한다.
- `app/api/**`: 지도 API는 지도·GIS Agent, 실거래·시장 API는 데이터·차트 Agent가 소유한다.
- `public/data/boundaries/**`: 지도·GIS Agent만 수정한다.
- 테스트 파일: QA Agent가 소유하되 기능 Agent가 자기 기능의 단위 테스트를 함께 추가할 수 있다.
- 배포 파일과 환경변수 이름: 통합 관리자와 QA·릴리스 Agent의 공동 검토가 필요하다.

권장 분리 대상은 다음과 같다.

```text
app/
  components/
    common/
    map/
    analysis/
    trades/
    responsive/
  hooks/
  lib/
    map/
    market/
  styles/
    tokens.css
    desktop.css
    tablet.css
    mobile.css
```

이 구조는 즉시 강제하지 않는다. 구조 Agent의 감사와 통합 관리자 승인을 거쳐 점진적으로 이동한다.

## 5. Agent 보고 형식

각 Agent는 완료 시 다음을 반드시 남긴다.

1. 발견한 문제와 사용자 영향
2. 변경 또는 제안한 파일
3. 다른 Agent와 합의가 필요한 인터페이스
4. 유지한 기존 기능
5. 실행한 검사와 결과
6. 남은 위험과 TODO
7. 병합 권장 순서

## 6. 금지 사항

- 다른 Agent의 브랜치나 worktree를 임의로 수정하지 않는다.
- 사용자 승인 없이 `main`에 병합하거나 Vercel에 배포하지 않는다.
- 여러 목적을 한 커밋에 섞지 않는다.
- API 키, 비밀번호, 토큰을 대화, 코드, Markdown, 로그, Git에 저장하지 않는다.
- 데이터가 없을 때 임의 값을 채우지 않는다.
- 반응형 문제를 요소 숨김만으로 해결하지 않는다.
- 공통 컴포넌트가 없다는 이유로 동일 UI를 화면마다 복제하지 않는다.

## 7. 공통 완료 기준

- `git diff --check`
- 변경 파일 ESLint
- vinext 프로덕션 빌드
- 데스크톱 1440px, 태블릿 1024px/768px, 모바일 390px 확인
- 지도 줌·선택 유지, 최근 실거래, 평수 필터, 상세 분석 확인
- 로딩, 오류, 빈 결과, 표본 부족 상태 확인
- 브라우저 콘솔 오류 확인
- 변경 브랜치와 병합 대상 SHA 기록

