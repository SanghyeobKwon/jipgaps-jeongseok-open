# 집값의 정석 — 저장소 전체 필수 작업 규칙

이 파일은 사람과 AI 작업자 모두에게 적용된다. 저장소 루트 이하의 모든 코드, 디자인, 데이터, 문서 변경은 아래 규칙을 따른다.

## 1. 변경 전 읽어야 할 문서

1. `BUSINESS.md`: 사업 목표, 사용자 가치, 데이터와 점수 정책
2. `PRODUCT.md`: 제품 포지셔닝과 핵심 원칙
3. `DESIGN.md`: 시각·반응형·상호작용 기준
4. `README.md`: 실행 방법과 기술 환경

문서 간 충돌이 있으면 사용자 최신 요청을 최우선으로 하고, 그다음 `AGENTS.md`, `BUSINESS.md`, `PRODUCT.md`, `DESIGN.md` 순서로 판단한다.

## 2. 제품 범위

- 대한민국 부동산만 다룬다.
- 기본 탐색 단계는 `전국 → 시·도 → 시·군·구 → 읍·면·동 → 단지 → 동·평형·층·방향`이다.
- 제품명은 `집값의 정석`으로 유지한다.
- 핵심 목적은 단기 투기 신호가 아니라 실거주와 매수 판단을 돕는 것이다.
- 직방, 아실, TradingView 등의 장점은 참고할 수 있지만 디자인, 문구, 코드와 데이터를 복제하지 않는다.

## 3. 절대 금지 사항

- API 키, 비밀번호, 이메일 로그인 정보, 토큰을 코드·Markdown·Git 커밋에 넣지 않는다.
- 브라우저에 노출되는 `NEXT_PUBLIC_*` 변수에 서버 비밀키를 넣지 않는다.
- 데이터가 없을 때 임의의 거래, 시설, 위치, 점수 또는 리뷰를 만들어 채우지 않는다.
- 예상값을 실측값처럼 표시하지 않는다.
- 거래 표본이 부족한 변동률을 확정적인 상승·하락 신호로 보여주지 않는다.
- 전국 지도에서 다른 지역의 동·역·단지 데이터를 재사용하지 않는다.
- 사용자 요청 없이 기존 기능, API 경로, 환경변수 이름 또는 배포 설정을 삭제하지 않는다.
- `git reset --hard`, 강제 푸시, 광범위 파일 삭제처럼 복구가 어려운 작업을 임의로 실행하지 않는다.

## 4. 코드 소유 영역과 병합 원칙

현재 핵심 파일의 역할은 다음과 같다.

- `app/page.tsx`: 화면 상태, 탐색 흐름, 차트·지도·온라인 임장 UI와 기능 연결
- `app/globals.css`: 전역 디자인, 반응형, 컴포넌트 스타일
- `app/api/**`: 공공 데이터, 지도·주소·시설·정책 연동
- `public/data/**`: 행정경계 등 정적 데이터
- `.openai/hosting.json`: Sites 프로젝트 연결 정보

디자인 작업자는 CSS와 표현 컴포넌트를 우선 수정하고, 데이터 로딩·선택 상태·API 호출 로직을 바꿀 때는 변경 이유를 남긴다. 기능 작업자는 기존 클래스명과 화면 구조를 불필요하게 깨지 않는다.

같은 파일을 여러 사람이 수정할 경우:

1. 최신 `main`을 기준으로 별도 브랜치에서 작업한다.
2. 한 커밋에는 하나의 목적만 담는다.
3. 충돌 시 양쪽 변경 내용을 비교해 기능과 디자인을 함께 보존한다.
4. 파일 전체 덮어쓰기로 충돌을 해결하지 않는다.
5. 병합 후 모바일과 데스크톱에서 핵심 화면을 다시 확인한다.

## 5. URL과 화면 상태

- 상단 탭은 URL 해시와 동기화한다. 예: `#map`, `#chart`, `#field`, `#policy`.
- 공유할 가치가 있는 하위 선택은 쿼리 문자열에 저장한다. 예: `?feature=noise#field`.
- 새로고침, 뒤로가기, 공유 링크에서도 사용자가 선택한 화면이 유지되어야 한다.
- 같은 화면으로 이동하는 링크는 서로 다른 상태 규칙을 만들지 않는다.
- 캐시 확인용 버전 파라미터는 배포 버전과 맞춘다.

## 6. 지역·주소·지도 규칙

- 공식 시·도 명칭을 사용한다. 예: `서울특별시`, `인천광역시`.
- `서울특별시 서울시노원구`처럼 광역명과 구 이름을 중복하지 않는다.
- 행정구역 코드는 실제 API 요청 코드와 일치해야 한다.
- 법정동과 행정동이 다르면 내부 데이터의 기준을 명시하고 화면 표기를 정규화한다.
- 지역 우선순위는 가나다순만 사용하지 않는다. 서울에서는 강남구 등 사용 빈도와 시장 중요도를 고려하되, 정렬 기준을 사용자에게 보여준다.
- 전국·시도·구·동 단계에서는 행정경계 중심의 개요 지도를 사용하고, 단지·건물 단계에서 실제 도로 지도를 사용한다.
- 실제 지도 라벨과 가격 마커가 겹치지 않도록 줌 단계별 표시 밀도를 조절한다.
- 건물 마커는 가격을 먼저 보여주고 아파트·오피스텔·연립·다세대 등 주거 유형을 구분한다.
- 특정 지역에 속하지 않는 역, 동, 단지를 표시하지 않는다. 좌표와 주소의 지역 포함 여부를 검증한다.

## 7. 실거래와 차트 규칙

- 차트의 기본 시간 단위는 월이다.
- 기간 선택은 최소 3개월, 6개월, 1년 이상의 범위를 지원한다.
- 시장·단지 순위의 기본 비교 기간은 최근 분기다.
- 가격은 거래 유형, 주택 유형, 단지, 건물 동과 평형을 가능한 범위에서 분리한다.
- 변동률은 비교 구간 양쪽의 표본이 충분할 때만 계산한다.
- 평균보다 중위가격을 우선 사용하며 단위는 화면에 명시한다.
- 거래량, 기준 월, 표본 수와 데이터 출처를 숨기지 않는다.
- 목록을 선택하면 동일 대상을 기준으로 상세 차트, 거래 내역, 지도와 평가가 갱신되어야 한다.

## 8. 온라인 임장과 점수 규칙

- 온라인 임장은 지역, 단지, 동·호수, 생활의 4단계 구조를 유지한다.
- 주요 기능에는 교통, 도보, 시간대, 소음, 일조, 조망, 주차, 관리비, 출퇴근, 생활권이 포함된다.
- 소음원은 도로, 버스, 지하철, 철도, 비행기, 상가, 학교, 공사장처럼 구체적으로 분류한다.
- 시간대 분석은 최소 06시, 09시, 12시, 18시, 22시, 01시를 지원한다.
- 실측 데이터가 없으면 `예상 소음`, `추정`, `베타`로 표시하고 계산 근거를 제공한다.
- 종합점수만 보여주지 말고 교통·생활편의·학군·일조·조망·보행·소음·관리비·주차 세부 점수를 함께 보여준다.
- 점수는 데이터가 실제로 연결된 항목으로만 계산한다. 연결 예정 항목을 임의의 숫자로 채우지 않는다.
- 가격 평가는 감정평가 또는 투자 권유가 아님을 명시한다.

## 9. UI와 접근성 규칙

- 전체 디자인은 밝고 정돈된 화면을 유지한다.
- 흰색·따뜻한 회색 표면과 블루 강조색을 기본으로 사용한다.
- 모바일 첫 화면에서 현재 선택 위치와 다음 행동이 즉시 보여야 한다.
- 주요 버튼의 터치 높이는 최소 44px로 한다.
- 탭의 활성 상태와 이동하는 하단 표시선을 명확히 유지한다.
- 중요한 기능을 수평 스크롤 안에만 숨기지 않는다. 모바일에는 바로가기 또는 명시적 힌트를 제공한다.
- 카드 남발을 피하고 표, 리스트와 넓은 단일 표면을 우선한다.
- 텍스트 대비, 키보드 포커스, 버튼 이름과 `aria` 정보를 유지한다.
- 빈 상태, 로딩, 오류, 표본 부족 상태는 서로 다르게 표현한다.

## 10. API와 보안 규칙

- 클라이언트는 서버 API 경로를 호출하고 비밀키를 직접 사용하지 않는다.
- 비밀값은 로컬 `.env.local`과 호스팅 환경변수에만 저장한다.
- `.env.example`에는 키 이름과 설명만 두고 실제 값을 넣지 않는다.
- 외부 API 응답은 실패, 지연, 빈 결과와 스키마 변경을 방어한다.
- 사용자의 검색어와 주소는 인코딩하고 외부 응답을 신뢰하지 않는다.
- 호출량이 큰 전국 데이터는 캐시, 배치, 사전 집계를 검토한다.
- 로그에 토큰, 전체 인증키, 개인정보를 출력하지 않는다.
- 노출된 키가 발견되면 문서에서 가리는 것으로 끝내지 말고 즉시 폐기·재발급한다.

## 11. 변경 작업 절차

1. 작업 전 `git status`로 사용자 변경사항을 확인한다.
2. 관련 코드를 `rg`로 찾아 변경 범위를 좁힌다.
3. 기존 기능과 선택 상태를 보존하면서 최소 범위로 수정한다.
4. 포맷·타입·린트·빌드 중 프로젝트에서 가능한 검사를 실행한다.
5. 핵심 사용자 흐름을 실제 브라우저에서 확인한다.
6. 변경 파일만 커밋하고 목적이 드러나는 메시지를 사용한다.
7. GitHub와 배포 소스가 동일한 커밋인지 확인한다.
8. 사이트 변경은 새 버전으로 배포하고 운영 URL에서 재검증한다.
9. 완료 보고에는 변경 내용, 검증 결과, 버전과 직접 확인 링크를 포함한다.

## 12. 검증 기준

최소한 다음 항목을 확인한다.

- `git diff --check`
- 변경한 TypeScript/React 파일의 ESLint
- vinext 프로덕션 빌드
- 브라우저 콘솔 오류 여부
- 모바일 폭에서 내비게이션, 지역 선택, 지도, 차트와 온라인 임장
- 공유 URL을 새로 열었을 때 화면과 하위 선택 유지
- API 오류와 데이터 없음 상태의 사용자 안내

저장소 전체 린트가 생성물이나 기존 오류로 실패하면 이를 숨기지 말고, 변경 파일 검사와 빌드를 별도로 실행한 뒤 기존 오류의 위치를 기록한다.

## 13. Git과 배포

- 기본 브랜치는 `main`이다.
- GitHub 저장소는 <https://github.com/AI-PESL-Seminar/jipgaps-jeongseok>이다.
- 운영 사이트는 <https://jipgaps-jeongseok.sueb4509.chatgpt.site>이다.
- `.openai/hosting.json`에 기존 `project_id`가 있으면 반드시 재사용한다.
- 같은 프로젝트를 새로 만들지 않는다.
- 배포 전에 소스 저장소에 현재 커밋을 올리고, 그 커밋으로 만든 빌드만 버전으로 저장한다.
- 환경변수 변경은 호스팅 설정에 적용하고 새 버전을 배포해야 효력이 생긴다.
- 문서만 변경한 경우 운영 사이트 재배포는 필요하지 않지만 GitHub에는 반영한다.

## 14. 완료의 정의

코드가 작성된 것만으로 완료되지 않는다. 다음 조건을 모두 만족해야 한다.

- 사용자의 요청이 실제 화면이나 문서에 반영됐다.
- 기존 주요 기능이 유지된다.
- 데이터 출처와 상태가 오해 없이 표시된다.
- 모바일과 데스크톱에서 사용할 수 있다.
- 검증 가능한 빌드 또는 문서 검사가 끝났다.
- GitHub와 필요한 경우 운영 배포까지 최신 상태다.

불확실한 데이터나 정책은 추측으로 채우지 말고 `확인 필요`로 표시한다.
